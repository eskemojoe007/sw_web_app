// import { shallowMount, createLocalVue, mount } from '@vue/test-utils';
// import { mount } from '@vue/test-utils';
// eslint-disable-next-line import/no-unresolved
// import MultiDate from '@/components/MultiDate.vue';
// import Vuetify from 'vuetify';

describe('MultiDate.vue', () => {
  // let localVue;
  // let wrapper;
  // beforeAll(() => {
  //   localVue = createLocalVue();
  //   localVue.use(Vuetify, {});
  //   wrapper = mount(MultiDate, { localVue });
  // });

  it('Dummy Test', () => {
    // const msg = 'Departure Dates';
    // wrapper.setProps({ msg });
    expect(1).toBe(1);
    // expect(wrapper.props().msg).toBe(msg);
  });
  // it('renders the text property when passed', () => {
  //   const msg = 'Departure Dates';
  //   // wrapper.setProps({ msg });
  //   expect(wrapper.html()).toBe(msg);
  //   // expect(wrapper.props().msg).toBe(msg);
  // });


  // it('render props.author when passed', () => {
  //   const msg = 'new message';
  //
  //   wrapper.setProps({ author: msg });
  //   expect(wrapper.html()).toMatch(msg);
  //   expect(wrapper.props().author).toBe(msg);
  // });
});
